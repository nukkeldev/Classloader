{
  "name": "classloader",
  "version": "0.0.1",
  "description": "A simple utility to schedule adding / removing classes; intended for CSS animation sequences",
  "main": "src/index.ts",
  "author": "nukkeldev",
  "license": "MIT",
  "scripts": {
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "watch": "tsc -w",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  },
  "dependencies": {

  },
  "devDependencies": {
    "gts": "^3.1.1",
    "typescript": "^5.0.4",
    "@types/node": "^14.11.2"
  }
}
